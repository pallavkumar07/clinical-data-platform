# Copy to .env and adjust. All vars are prefixed with CDP_.
CDP_ENVIRONMENT=development
CDP_DEBUG=true

# Medplum (hosted FHIR backend) — see docs/medplum.md for how to get these.
CDP_MEDPLUM_BASE_URL=https://api.medplum.com/
CDP_MEDPLUM_CLIENT_ID=
CDP_MEDPLUM_CLIENT_SECRET=
