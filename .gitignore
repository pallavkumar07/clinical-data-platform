# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtual environments
.venv/
venv/
env/

# Environment / secrets — all env files are ignored, including the example,
# because it contains live credentials on this machine. New users should
# follow docs/medplum.md to create their own .env.
.env
.env.*

# Credentials & secret material — never commit these to a public repo.
*.pem
*.key
*.p12
*.pfx
*.keystore
*.jks
id_rsa
id_dsa
id_ecdsa
id_ed25519
*.crt
*.cer
secrets.*
*secret*.json
*secret*.yaml
*secret*.yml
credentials
credentials.*
*credentials*.json
.netrc
.pgpass
*.token
service-account*.json
gcp-*.json
aws-credentials*
.aws/
.ssh/

# Editors / OS
.vscode/
.idea/
.DS_Store

# Coverage
.coverage
htmlcov/
